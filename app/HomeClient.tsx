'use client';
import { ArrowLeft,ArrowRight,BarChart3,Bell,CalendarDays,Camera,ChevronLeft,ChevronRight,CirclePlus,DoorOpen,Grid2X2,Hexagon,Lightbulb,Pencil,Play,Search,Star,UserRound,Video,Calculator,NotebookPen,StickyNote,Presentation } from 'lucide-react';
import {ChangeEvent,CSSProperties,MouseEvent,useEffect,useMemo,useRef,useState} from 'react';
import type { HomeContent } from '../lib/cms/types';

const baseSideIcons=[Grid2X2,CirclePlus,CalendarDays,BarChart3,UserRound,Bell,Video,Hexagon,DoorOpen];
const extraSidebarItems=[{label:'Calculadora',Icon:Calculator},{label:'Anotar',Icon:NotebookPen},{label:'Anotações',Icon:StickyNote},{label:'Criar slides',Icon:Presentation}];
const cardIcon=(slug:string)=>slug==='recordings'?Play:slug==='insights'?Lightbulb:BarChart3;const cardClass=(index:number)=>index===0?'card-one':index===1?'card-two':'card-three';const PROFILE_AVATAR_KEY='zyvo-profile-avatar';
export default function HomeClient({content}:{content:HomeContent}){
  const[expanded,setExpanded]=useState(false);const[name,setName]=useState(content.profile.name);const[editingName,setEditingName]=useState(false);const[avatar,setAvatar]=useState<string|null>(content.profile.avatarUrl||null);const[slide,setSlide]=useState(0);const[loaded,setLoaded]=useState(false);const[notificationsOpen,setNotificationsOpen]=useState(false);const fileRef=useRef<HTMLInputElement>(null);const notificationRef=useRef<HTMLDivElement>(null);const notificationButtonRef=useRef<HTMLButtonElement>(null);
  const slides=useMemo(()=>{const active=content.carousel.filter(x=>x.isActive);return active.length?active:content.carousel;},[content.carousel]);
  useEffect(()=>{try{const savedAvatar=window.localStorage.getItem(PROFILE_AVATAR_KEY);if(savedAvatar)setAvatar(savedAvatar)}catch{}},[]);useEffect(()=>{requestAnimationFrame(()=>setLoaded(true));if(!slides.length)return;const t=setInterval(()=>setSlide(s=>(s+1)%slides.length),content.carouselIntervalMs||4000);return()=>clearInterval(t)},[slides.length,content.carouselIntervalMs]);useEffect(()=>setSlide(0),[slides]);useEffect(()=>{if(!notificationsOpen)return;const onOutside=(e:globalThis.MouseEvent)=>{const target=e.target as Node;if(notificationRef.current?.contains(target)||notificationButtonRef.current?.contains(target))return;setNotificationsOpen(false)};document.addEventListener('mousedown',onOutside);return()=>document.removeEventListener('mousedown',onOutside)},[notificationsOpen]);
  const changeAvatar=(e:ChangeEvent<HTMLInputElement>)=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>{const nextAvatar=String(r.result);setAvatar(nextAvatar);try{window.localStorage.setItem(PROFILE_AVATAR_KEY,nextAvatar)}catch{}};r.readAsDataURL(f)};const handleSidebarClick=(e:MouseEvent<HTMLElement>)=>{const target=e.target as HTMLElement;if(target.closest('button,input,a'))return;setExpanded(v=>!v)};const go=(d:number)=>setSlide(s=>slides.length?(s+d+slides.length)%slides.length:0);const current=slides[slide];const heroStyle=content.hero.imageUrl?{backgroundImage:`url(${content.hero.imageUrl})`}:undefined;const titleLines=content.hero.title.split('\n');const firstName=(name.trim().split(/\s+/)[0]||content.profile.name.trim().split(/\s+/)[0]||'Sandro');
  return <main className={`app-shell ${loaded?'loaded':''}`}>{content.hero.imageUrl&&<div className="hero-media" style={heroStyle}/>}<aside className={`sidebar ${expanded?'expanded':''}`} onClick={handleSidebarClick}><div className="avatar-wrap"><button className="avatar-button" onClick={()=>fileRef.current?.click()}><div className="avatar" style={avatar?{backgroundImage:`url(${avatar})`}:undefined}>{!avatar&&name.split(/\s+/).map(x=>x[0]).slice(0,2).join('')}</div><span className="avatar-camera"><Camera size={11}/></span></button><input ref={fileRef} className="avatar-input" type="file" accept="image/*" onChange={changeAvatar}/>{expanded&&<div className="avatar-meta">{editingName?<input className="name-input" value={name} autoFocus onChange={e=>setName(e.target.value)} onBlur={()=>setEditingName(false)}/>:<button className="name-edit" onClick={()=>setEditingName(true)}><strong>{name}</strong><Pencil size={12}/></button>}<span>{content.profile.planLabel}</span></div>}</div><div className="side-nav">{content.navigation.sidebar.map((label,i)=>{const Icon=baseSideIcons[i]||Grid2X2;return <button className={`side-item ${i===0?'active':''}`} key={`${label}-${i}`}><Icon/><span>{label}</span></button>})}{extraSidebarItems.map(({label,Icon})=><button className="side-item" key={label}><Icon/><span>{label}</span></button>)}</div><button className="sidebar-toggle" onClick={()=>setExpanded(v=>!v)} aria-label={expanded?'Recolher menu':'Expandir menu'}>{expanded?<ChevronLeft/>:<ChevronRight/>}</button></aside>
  <section className={`content ${expanded?'shifted':''}`}><header className="topbar"><div className="search-box"><Search size={27}/><span>{content.navigation.searchPlaceholder}</span><kbd>⌘ K</kbd></div><nav className="topnav">{content.navigation.top.map((label,i)=><a className={i===0?'current':''} key={label}>{label}</a>)}</nav><div className="next-meeting"><span>{content.nextMeeting.label}</span><strong>{content.nextMeeting.dateTime}</strong></div><div className="notification-wrap"><button ref={notificationButtonRef} className={`notification-button ${notificationsOpen?'active':''}`} aria-label="Notificações" aria-expanded={notificationsOpen} onClick={()=>setNotificationsOpen(v=>!v)}><Bell/></button>{notificationsOpen&&<div ref={notificationRef} className="notification-panel"><div className="notification-head"><strong>Notificações</strong><span>Hoje</span></div><div className="notification-item"><span className="notification-mark"/><div><strong>Próxima reunião</strong><p>{content.nextMeeting.dateTime}</p></div></div><div className="notification-item"><span className="notification-mark muted"/><div><strong>Performance atualizada</strong><p>{content.hero.performancePercent}% de performance nas reuniões.</p></div></div></div>}</div></header><section className="hero-grid"><div className="hero-copy"><div className="hero-greeting">Bem-vindo, <strong>{firstName}!</strong></div><div className="eyebrow">{content.hero.eyebrow}</div><h1>{titleLines.map((line,i)=><span key={i}>{line}{i<titleLines.length-1&&<br/>}</span>)}</h1><div className="rating-line"><div className="stars">{[0,1,2,3,4].map(n=><Star key={n} size={20} fill="currentColor"/>)}</div><span>{content.hero.ratingText}</span></div><div className="hero-score"><b>{content.hero.performancePercent}%</b> {content.hero.performanceLabel}</div><div className="progress"><i style={{'--p':`${content.hero.performancePercent}%`} as CSSProperties}/></div><div className="ticks"/><div className="hero-actions"><button className="primary-btn"><Video size={22}/>{content.hero.primaryButton}<ArrowRight size={22}/></button><button className="secondary-btn"><Play size={25}/>{content.hero.secondaryButton}<ArrowRight size={22}/></button></div></div><div className="hero-feature"><div className="feature-card">{current?.imageUrl?<div className="feature-photo" key={current.id||slide} style={{backgroundImage:`url(${current.imageUrl})`}}/>:<div className="feature-logo" key={slide}>{slide===0?'Z':slide===1?'AI':String(content.hero.performancePercent)}</div>}<div className="feature-text" key={`t${slide}`}>{current?.title||'Mais do que reuniões.'}<br/>{current?.subtitle||'Evolução.'}</div><div className="slider-dashes">{slides.map((_,i)=><i key={i} className={i===slide?'on':''}/>)}</div><div className="feature-arrows"><button onClick={()=>go(-1)}><ArrowLeft/></button><button onClick={()=>go(1)}><ArrowRight/></button></div></div></div></section><section className="cards-grid">{content.cards.filter(c=>c.isActive).sort((a,b)=>a.sortOrder-b.sortOrder).map((card,index)=>{const Icon=cardIcon(card.slug);const overlay=index===1?'linear-gradient(90deg,rgba(238,244,247,.88),rgba(238,244,247,.05))':'linear-gradient(90deg,rgba(238,244,247,.9),rgba(238,244,247,.18))';return <article className={`info-card ${cardClass(index)}`} key={card.slug} style={card.imageUrl?{backgroundImage:`${overlay},url(${card.imageUrl})`}:undefined}><Icon className={`card-icon ${card.slug==='recordings'?'circled':''}`}/><h2>{card.title}</h2><p>{card.description}</p><strong>{card.percentage}%</strong><div className="card-progress"><i style={{'--p':`${card.percentage}%`} as CSSProperties}/></div><button className="round-go" aria-label={card.ctaLabel}><ArrowRight/></button></article>})}</section></section></main>;
}
